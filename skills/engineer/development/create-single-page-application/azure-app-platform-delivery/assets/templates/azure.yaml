name: __APP_NAME__

metadata:
  template: azure-app-platform-delivery

services:
  __SERVICE_NAME__:
    project: .
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile

hooks:
  postprovision:
    shell: sh
    run: ./scripts/azure/postprovision.sh
    continueOnError: false
