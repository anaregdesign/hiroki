interface:
  display_name: "React Router Prisma Architecture"
  short_description: "Own app-code architecture, route boundaries, UI structure, and verification"
  default_prompt: "Use $react-router-prisma-app-architecture when the work is primarily React Router routes, loaders or actions, component boundaries, use cases, domain models, Prisma-backed server code, Fluent UI screens, responsive UI, charts, Playwright verification, or app-structure refactoring. Do not use it for `/docs/spec` planning, Azure platform setup, app registration, end-user auth contract design, or release delivery."
